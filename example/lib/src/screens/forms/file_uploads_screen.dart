import 'package:flutter/material.dart';
import 'package:sentinels_main_front_ui/sentinels_main_front_ui.dart';

class FileUploadsScreen extends StatelessWidget {
  const FileUploadsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('FileUploadsScreen Placeholder'));
  }
}
