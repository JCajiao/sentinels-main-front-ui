name: sentinels_main_front_ui_example
description: Example app for Sentinels Main Front UI package.
publish_to: none

environment:
  sdk: ^3.3.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  sentinels_main_front_ui:
    path: ../
  go_router: ^14.0.0
  fl_chart: ^0.69.0
  table_calendar: ^3.1.2
  flutter_map: ^7.0.2
  latlong2: ^0.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
